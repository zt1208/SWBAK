# 交换机配置备份工具 - 使用说明

## 一、简介

本工具用于通过 SSH/Telnet 协议批量备份华为、华三（H3C）、锐捷（Ruijie）交换机的运行配置。支持自动识别厂商、多线程并发备份、批量导入设备清单、配置版本对比、全文搜索、定时备份、邮件通知、自动导出报告等功能。

**适用场景：** 企业网络运维、数据中心交换机配置定期备份与审计。

---

## 二、环境要求与安装

### 2.1 系统要求

- 操作系统：Windows 7/10/11（64位）
- Python：3.8 及以上版本
- 网络：管理终端需能 SSH/Telnet 访问目标交换机

### 2.2 一键启动

双击 `start.bat` 即可，脚本会自动完成以下操作：

1. 检查 Python 是否安装
2. 安装依赖库（netmiko、openpyxl、xlsxwriter）
3. 启动主程序

> 如首次使用提示 Python 未找到，请前往 [python.org](https://www.python.org/downloads/) 下载安装，安装时勾选 **"Add Python to PATH"**。

### 2.3 手动安装依赖

```bash
pip install -r requirements.txt
```

依赖列表：

| 库名 | 用途 |
|------|------|
| netmiko >= 4.2.0 | SSH/Telnet 多厂商交换机连接 |
| openpyxl >= 3.1.2 | Excel 批量导入读取 |
| xlsxwriter >= 3.1.2 | Excel 报告导出 |

---

## 三、快速开始

### 3.1 启动程序

双击 `start.bat` 或执行 `python main.py`。

### 3.2 添加设备

点击工具栏 **"添加设备"** 按钮，在弹出的对话框中选择以下方式之一：

| 标签页 | 说明 |
|--------|------|
| **手动添加** | 逐台填写 IP、端口、协议、用户名、密码等 |
| **批量录入** | 粘贴多行 `IP 用户名 密码`，一次性添加多台 |
| **统一密码导入** | 粘贴多个 IP，统一输入一组账号密码 |
| **从文件导入** | 选择 Excel/CSV/TXT 文件批量导入 |

> 厂商选择 **"自动识别"** 时，工具会在登录成功后自动判断设备类型，无需手动选择。

### 3.3 执行备份

点击工具栏 **"开始备份"** 按钮，程序将：

1. 多线程并发连接所有设备
2. 自动识别厂商类型
3. 抓取运行配置
4. 按分组/设备主机名保存配置文件
5. 备份完成后自动导出 Excel 报告

### 3.4 查看结果

- **设备列表：** 备份完成后自动按状态（成功/失败）和 IP 地址排序显示
- **日志区：** 实时显示连接过程、厂商识别、配置输出摘要
- **备份报告：** 自动导出到 `reports/` 目录，成功和失败设备分两个 Sheet

---

## 四、功能详解

### 4.1 设备管理

#### 添加设备

工具栏点击 **"添加设备"**，支持 4 种方式：

**① 手动添加**

逐台填写设备信息：

| 字段 | 说明 | 示例 |
|------|------|------|
| 设备名称 | 自定义标识（可空，备份时用设备实际主机名） | 核心交换机-01 |
| IP 地址 | 交换机管理 IP | 10.8.0.30 |
| 端口 | SSH 默认 22，Telnet 默认 23 | 22 |
| 厂商 | auto(自动识别) / huawei / h3c / ruijie | auto |
| 协议 | ssh / telnet | ssh |
| 用户名 | 登录账号 | admin |
| 密码 | 登录密码 | Admin@123 |
| 特权密码 | 部分设备需要（如锐捷 enable） | Admin@enable |
| 分组 | 设备分组，用于按文件夹归类备份 | 核心机房 |
| 超时 | 连接超时（秒） | 30 |

> 账号密码会自动保存，下次添加设备时自动填充，重启不丢失。

**② 批量录入**

在文本框中粘贴多行，每行一台设备，格式：

```
IP地址 用户名 密码
10.8.0.30 admin Admin@123
10.8.0.31 admin Admin@123
```

**③ 统一密码导入**

适用于所有设备使用相同账号密码的场景。粘贴 IP 列表（每行一个），统一填写一组用户名/密码。

**④ 从文件导入**

支持 Excel(.xlsx/.xls)、CSV、TXT 格式。表头兼容中英文，列顺序不限。

识别的表头字段：

| 字段 | 兼容表头 |
|------|----------|
| 设备名称 | name / 设备名称 / 名称 / hostname / 交换机名 |
| IP 地址 | ip / IP地址 / 管理IP / 地址 / host |
| 端口 | port / 端口 / 端口号 / ssh端口 |
| 厂商 | vendor / 厂商 / 品牌 / 设备类型 |
| 协议 | protocol / 协议 / 连接方式 |
| 用户名 | username / 用户名 / 账号 |
| 密码 | password / 密码 / 口令 |
| 特权密码 | enable / 特权密码 / enable_password |
| 分组 | group / 分组 / 区域 / 机房 |
| 超时 | timeout / 超时 / 超时时间 |

> 表头识别失败时，工具会自动扫描数据列，通过 IP 正则表达式回退识别 IP 列。

#### 批量导入模板

菜单 **文件 → 导出导入模板** 可生成标准模板文件。

#### 设备列表操作

| 操作 | 说明 |
|------|------|
| 编辑设备 | 双击列表中的设备行 |
| 删除设备 | 选中设备后点击"删除"按钮 |
| 清空列表 | 菜单 文件 → 清空设备列表 |
| 保存清单 | 菜单 文件 → 保存设备清单（保存到 devices.json） |
| 打开清单 | 菜单 文件 → 打开设备清单 |

### 4.2 备份功能

#### 启动备份

| 入口 | 说明 |
|------|------|
| 工具栏"开始备份" | 备份列表中所有设备 |
| 菜单 备份 → 备份选中设备 | 仅备份列表中选中的设备 |

#### 多线程并发

- 并发线程数可在 **设置** 中调整（默认 10）
- 1000 台设备建议线程数 10-20（同网段）或 20-50（跨网段）
- 线程数过高可能影响设备管理面性能

#### 厂商自动识别

工具采用多级识别策略，无需手动选择厂商：

1. **提示符快速判断**：通过 `find_prompt()` 获取设备提示符，锐捷设备可直接识别（0.5 秒）
2. **Version 命令确认**：发送 `display version` / `show version`，通过输出内容判断厂商
3. **逐厂商回退**：如自动识别失败，依次尝试华为/华三/锐捷方式登录

#### 配置文件保存

配置文件按 **分组/设备实际主机名** 两层目录结构保存：

```
backups/
  └── 核心机房/                          ← 分组文件夹
      ├── Core-SW-01/                    ← 设备实际主机名
      │   ├── Core-SW-01_20260813_103650.txt   ← 带时间戳的备份
      │   └── latest.txt                       ← 最新备份（覆盖写入）
      └── Core-SW-02/
          ├── Core-SW-02_20260813_103650.txt
          └── latest.txt
  └── 接入层/
      └── Access-SW-01/
          ├── Access-SW-01_20260813_103650.txt
          └── latest.txt
```

- 配置文件格式为 `.txt`
- 每次备份生成带时间戳的文件 + 一份 `latest.txt`（方便对比）
- 设备实际主机名从设备配置中自动获取

#### 备份结果排序

备份完成后，设备列表自动按以下规则排序：

1. **成功设备**（按 IP 地址从小到大）
2. **失败设备**（按 IP 地址从小到大）
3. **其他状态设备**（按 IP 地址从小到大）

IP 按段比较（10.8.0.9 < 10.8.0.10），不是字符串比较。

#### 自动导出报告

每次备份完成后自动导出 Excel 报告到 `reports/` 目录：

- 文件名：`backup_report_YYYYMMDD_HHMMSS.xlsx`
- **Sheet1**：成功设备（IP 升序），标签名如 `成功 (856台)`
- **Sheet2**：失败设备（IP 升序），标签名如 `失败 (12台)`
- **Sheet3**：其他状态设备（如有）

每行包含：设备名称、IP、厂商、协议、端口、分组、状态、备份时间、实际主机名、备份文件路径、详情。

也可通过菜单 **工具 → 导出备份报告(Excel)** 手动导出。

### 4.3 配置对比

菜单 **工具 → 配置对比**，选择设备和两个版本，一键对比差异：

- 以 unified diff 格式显示配置差异
- 绿色标记新增行，红色标记删除行
- 显示新增/删除行数统计

支持两层目录结构（分组/设备名），下拉框中显示为 `分组/设备名`。

### 4.4 配置搜索

菜单 **工具 → 配置搜索**，在所有备份配置中搜索关键字：

- 递归搜索所有分组和设备目录
- 显示匹配的文件路径、行号、行内容
- 支持模糊搜索（不区分大小写）

### 4.5 定时备份

菜单 **设置 → 定时备份**，支持两种模式：

| 模式 | 说明 | 示例 |
|------|------|------|
| 间隔模式 | 每隔 N 小时执行一次 | 每 24 小时备份一次 |
| 定时模式 | 每天 N 点执行 | 每天凌晨 2:00 自动备份 |

定时任务在后台线程运行，程序需保持开启状态。

### 4.6 邮件通知

菜单 **设置 → 邮件通知**，备份完成后自动发送结果汇总邮件：

- 支持 SSL（端口 465）和 STARTTLS（端口 587）
- 邮件包含 HTML 格式的备份结果表格
- 收件人多个时用逗号分隔

### 4.7 日志系统

日志区实时显示备份过程，包括：

- 设备连接状态（正在登录、连接成功/失败）
- 厂商识别过程（提示符判断、version 输出摘要）
- 配置输出摘要（总行数、字符数、前 10 行内容）
- 备份文件路径和文件大小
- 异常错误信息

日志示例：

```
[10:36:09] [10.8.0.30] 自动识别: 正在登录 (ssh)...
[10:36:11] [10.8.0.30] 提示符识别: 锐捷 (ruijie)
[10:36:11] [10.8.0.30] version> Ruijie Series Router/Switch Software
[10:36:12] [10.8.0.30] 执行: show running-config
[10:8.0.30] 配置输出: 327 行 / 48231 字符
[10.8.0.30] cfg> !Software Version V200R019C10SPC500
[10.8.0.30] cfg> #
[10.8.0.30] cfg> sysname Core-SW-01
...
[10:36:13] [10.8.0.30] 备份成功 -> backups\核心机房\Core-SW-01\Core-SW-01_20260813_103650.txt (47.1 KB)
```

---

## 五、设置说明

菜单 **设置 → 设置** 打开设置对话框：

### 5.1 基本设置

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| 备份目录 | 配置文件保存根目录 | backups/ |
| 并发线程数 | 多线程备份的并发数 | 10 |
| 连接超时 | SSH/Telnet 连接超时（秒） | 30 |

### 5.2 凭据记忆

添加设备时输入的账号密码会自动保存，下次添加设备时自动填充。以下信息会持久化存储：

- 上次输入的用户名、密码、特权密码
- 上次选择的厂商、协议、端口、分组

### 5.3 配置文件

应用配置保存在 `config.json`，设备列表保存在 `devices.json`，均在程序目录下。

---

## 六、文件结构

```
成大交换机备份/
├── main.py              # 主程序入口 (GUI 界面)
├── backup_engine.py     # 备份引擎 (SSH/Telnet 连接、厂商识别、配置抓取)
├── device.py            # 设备数据模型与厂商命令定义
├── importer.py          # 批量导入模块 (Excel/CSV/TXT 解析)
├── gui_dialogs.py       # GUI 对话框 (添加设备、批量导入、配置对比等)
├── config_compare.py    # 配置对比与全文搜索
├── notifier.py          # 邮件通知模块
├── scheduler.py         # 定时备份调度器
├── app_config.py        # 应用配置管理 (JSON 持久化)
├── requirements.txt     # Python 依赖列表
├── start.bat            # 一键启动脚本
├── config.json          # 应用配置文件 (运行后自动生成)
├── devices.json         # 设备列表文件 (保存后自动生成)
├── backups/             # 配置备份根目录
│   └── 分组名/
│       └── 设备主机名/
│           ├── 设备名_时间戳.txt
│           └── latest.txt
└── reports/             # 备份报告目录 (自动生成)
    └── backup_report_时间戳.xlsx
```

---

## 七、支持的交换机厂商

| 厂商 | SSH device_type | Telnet device_type | 配置命令 | 关闭分页命令 |
|------|-----------------|-------------------|----------|-------------|
| 华为 (Huawei) | huawei | huawei_telnet | `display current-configuration` | `screen-length 0 temporary` |
| 华三 (H3C) | hp_comware | hp_comware_telnet | `display current-configuration` | `screen-length disable` |
| 锐捷 (Ruijie) | ruijie_os | ruijie_os_telnet | `show running-config` | `terminal length 0` |

---

## 八、常见问题

### Q1: 双击 start.bat 打不开？

- 确认已安装 Python 3.8+，且安装时勾选了 "Add Python to PATH"
- 在命令行中手动执行 `python main.py` 查看具体报错

### Q2: 批量导入识别不到 IP 地址？

- 确保表头包含 IP 相关字段（如 "IP地址"、"IP"、"管理IP"、"host"）
- 工具会自动回退扫描数据列，通过 IP 正则匹配识别
- 如仍无法识别，检查数据格式是否为标准 IPv4（如 10.8.0.30）

### Q3: 登录成功但无法识别厂商？

- 工具会依次尝试提示符判断 → version 命令 → 逐厂商登录
- 如三种方式均失败，检查设备是否修改了默认提示符
- 可在添加设备时手动指定厂商类型

### Q4: 备份速度慢？

- 在设置中调整并发线程数（建议 10-20）
- 厂商自动识别已优化为提示符优先（0.5 秒），非必要不手动选择厂商
- 网络延迟较高的设备可适当增加连接超时

### Q5: 批量备份 1000 台会影响设备吗？

- **不会**。工具只执行只读命令（display/show），不修改或保存配置
- 每台设备仅建立 1 个连接，执行查询后立即断开
- 建议线程数不超过 20，避开业务高峰期

### Q6: 配置文件保存在哪里？

- 默认保存在程序目录下的 `backups/` 文件夹
- 按 **分组/设备实际主机名/** 两层目录组织
- 可在设置中修改备份目录

### Q7: 密码会丢失吗？

- 不会。账号密码保存在 `config.json` 和 `devices.json` 中
- 重启程序后自动加载，添加新设备时自动填充

### Q8: 如何对比两次备份的配置差异？

- 菜单 工具 → 配置对比
- 选择设备目录 → 选择旧版本和新版本 → 点击"对比"
- 差异以红绿色高亮显示

---

## 九、技术说明

### 厂商识别流程

```
登录设备 (autodetect 连接)
    │
    ├─ ① 提示符判断 (find_prompt, ~0.5s)
    │     锐捷: hostname> 或 hostname# → ruijie
    │
    ├─ ② Version 命令判断 (send_command_timing, ~3s)
    │     发送 display version / show version
    │     输出含 "Huawei" → huawei
    │     输出含 "H3C"/"Comware" → h3c
    │     输出含 "Ruijie" → ruijie
    │
    └─ ③ 逐厂商回退 (每种 15s 超时)
          依次用 huawei / h3c / ruijie 方式登录
          登录成功后用提示符+version 确认
```

### 多线程备份

使用 `ThreadPoolExecutor` 实现并发备份：

- 每台设备在独立线程中执行连接、识别、抓取、保存
- 线程数由设置控制，默认 10
- 备份进度实时更新到 UI

### 数据安全

- 工具仅执行只读命令，不修改设备配置
- 密码本地存储（明文 JSON），仅限本机使用
- 配置文件保存在本地，不上传任何远程服务器
